-- 코드를 입력하세요
SELECT 맛
FROM (
SELECT B.FLAVOR AS 맛, SUM(B.TOTAL_ORDER)+SUM(A.TOTAL_ORDER) AS 합
FROM JULY A, FIRST_HALF B
WHERE A.FLAVOR = B.FLAVOR
GROUP BY B.FLAVOR
ORDER BY 합 DESC
) 
WHERE ROWNUM <=3

;



